resources: "resources.yaml"
samples: "samples.tsv"
units: "units.tsv"

default_container: "docker://hydragenetics/common:0.1.1"

references:
  fasta: reference/reference.fasta

minimap2:
  index: reference/reference.mmi